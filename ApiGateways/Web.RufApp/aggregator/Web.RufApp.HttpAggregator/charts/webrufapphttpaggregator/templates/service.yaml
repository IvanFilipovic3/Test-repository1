apiVersion: v1
kind: Service
metadata:
  name: {{ template "webrufapphttpaggregator.fullname" . }}
  labels:
    app: {{ template "webrufapphttpaggregator.name" . }}
    chart: {{ template "webrufapphttpaggregator.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "webrufapphttpaggregator.name" . }}
    release: {{ .Release.Name }}
