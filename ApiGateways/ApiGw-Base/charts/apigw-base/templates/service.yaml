apiVersion: v1
kind: Service
metadata:
  name: {{ template "apigw-base.fullname" . }}
  labels:
    app: {{ template "apigw-base.name" . }}
    chart: {{ template "apigw-base.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "apigw-base.name" . }}
    release: {{ .Release.Name }}
