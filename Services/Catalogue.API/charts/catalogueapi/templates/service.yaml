apiVersion: v1
kind: Service
metadata:
  name: {{ template "catalogueapi.fullname" . }}
  labels:
    app: {{ template "catalogueapi.name" . }}
    chart: {{ template "catalogueapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "catalogueapi.name" . }}
    release: {{ .Release.Name }}
