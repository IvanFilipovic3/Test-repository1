apiVersion: v1
kind: Service
metadata:
  name: {{ template "appbackgroundtasks.fullname" . }}
  labels:
    app: {{ template "appbackgroundtasks.name" . }}
    chart: {{ template "appbackgroundtasks.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "appbackgroundtasks.name" . }}
    release: {{ .Release.Name }}
